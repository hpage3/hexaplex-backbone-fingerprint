# Legacy peptide_box Copy Note

The legacy peptide_box working tree was copied from:

$source

The .git metadata and __pycache__ folders were excluded so this repository can commit the legacy files as ordinary reference files.

Some OneDrive cloud-placeholder files could not be hydrated locally during the copy and returned: The cloud operation was unsuccessful. Those missing relative paths are listed in legacy/peptide_box_missing_onedrive_files.txt.
